syntax="proto3";//协议版本
option java_outer_classname="StudentPOJO";//生成的外部类名，同时也是文件名

message Student{//会在StudentPOJO外部类生成Student内部类。即真正发送的pojo对象
    int32 id=1;//Student类中有一个属性名字为id 类型为int32(protobuf类型)对应java的int，1表示属性序号，不是值
    string name=2;
}
